const multiline = `
  This is a
  multiline string.
`;

const arrayJoin = ['a', 'b'].join('-');

const binaryOp = 1 + 2 + 3;

const sum = 1 + 2 + '3';

const before = 'Hello' + 66 + 'World' + '77' + variable + func();
